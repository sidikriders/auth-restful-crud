<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>HOME | CMU </title>
  </head>
  <body>
    <div class="memo-cont">
      <h3>Memo list</h3>
      <br>
      <a href="/memo/new"><button type="button" name="buat-baru">Buat Baru</button></a>
      <br>
      <div class="memo-list">
        <%if (memo.length<1) {%>
          <p>masih belom ada memo yang kamu buat, silahkan <a href="/memo/new">buat baru.</a></p>
        <%} else {%>
          <ol>
            <%for (let i=0; i< memo.length; i++) {%>
              <li><a href="/memo/<%=memo[i]._id%>"><%=memo[i].title%></a> <a href="/memo/delete/<%=memo[i]._id%>" style="margin-left: 20px" onclick="return confirm('yakin hapus memo ini??')">delete</a></li>
            <%}%>
          </ol>
        <%}%>
      </div>
    </div>
    <div class="user-cont">
      <h3>User list</h3>
      <div class="user-list">
        <ul>
          <%for (let i = 0;i < user.length;i++) {%>
            <li><%=user[i].username%></li>
          <%}%>
        </ul>
      </div>
    </div>
    <br><br><br>
    <a href="/logout"><button type="button" name="button">Log Out</button></a>
  </body>
</html>
